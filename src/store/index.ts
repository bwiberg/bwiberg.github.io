import Vue from "vue";
import Vuex from "vuex";
import Queue from "utils/Queue";

Vue.use(Vuex);

const store = new Vuex.Store({
    state: {
    },
    getters: {
    },
    mutations: {
    },
    actions: {
    }
});

export default store;
